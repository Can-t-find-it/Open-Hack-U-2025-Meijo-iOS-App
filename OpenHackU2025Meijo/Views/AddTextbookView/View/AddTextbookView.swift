import SwiftUI

struct AddTextbookView: View {
    var body: some View {
        VStack {
            Text("AddTextbooksView")
        }
        .fullBackground()
    }
}

#Preview {
    AddTextbookView()
}
